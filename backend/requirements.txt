Django==6.0.7
djangorestframework==3.17.1
django-cors-headers==4.9.0
psycopg2-binary==2.9.12
python-decouple==3.8
dj-database-url==3.1.2

# Redis and Celery
redis==8.0.1
celery==5.6.3
# tzlocal is a celery runtime dep (imported in celery.utils.time) that celery
# declares unpinned, so pip resolves the latest. tzlocal 5.4.2 shipped as a
# broken/empty wheel (metadata only, no module code) that fails to import
# under Python 3.14, breaking `import celery` and thus all backend CI. Pin to
# the last known-good release. (oms-zkr3o)
tzlocal==5.4.4
django-redis==7.0.0
django-celery-results==2.6.0
flower==2.0.1

# QR Code and PDF generation
qrcode[pil]==8.2
reportlab==5.0.0
Pillow==12.3.0
# Server-side PNG charts for the monthly analytics pulse email (analytics app).
# Forced to the headless Agg backend in code so this does not pull GUI deps.
matplotlib==3.11.1
pyzbar==0.1.9  # Optional: for QR code validation (requires system zbar library)
pypdf==6.14.2   # PDF text/form extraction for inbound work-order ingestion
opencv-python-headless==5.0.0.93  # cv2.QRCodeDetector for image-based PDF ingestion

# Authentication
djangorestframework-simplejwt==5.5.1
django-passkey-auth==0.2.0

# Image handling
django-imagekit==6.1.0

# API Documentation
drf-spectacular==0.30.0

# Used by config/permission_matrix.py for the API permission matrix snapshot
# (gh #328). drf-spectacular pulls this in transitively today, but we pin it
# directly so the matrix loader does not depend on that.
PyYAML==6.0.3

# Utilities
python-slugify==8.0.4
requests==2.34.2
paho-mqtt==2.1.0
PyJWT==2.13.0
cryptography==49.0.0

# Email (Postmark via Anymail)
django-anymail[postmark]==15.0

# Error reporting / observability (Sentry). Replaces Highlight (oms-fjs)
# after that integration was ripped out; backend init is in
# `config/settings.py` and is a no-op unless SENTRY_DSN is set.
sentry-sdk[django,celery]==2.66.1

# Accounting ledger — double-entry engine (accounting app, Phase 1).
# django-hordak is the ledger core; it requires PostgreSQL (balance-enforcing
# triggers + balance views are Postgres-only). django-money/py-moneyed supply
# the MoneyField columns on hordak.Leg; django-mptt backs the account tree.
# hordak pulls django-import-export/django-extensions/tablib transitively —
# those are libraries, not INSTALLED_APPS. Pins resolve on Django 6.0 / Py3.13.
django-hordak==2.0.0
django-money==3.6.1
py-moneyed==3.0
django-mptt==0.18.0

# Testing and Code Coverage
coverage==7.15.2
pytest==9.1.1
pytest-django==4.12.0
pytest-cov==7.1.0
pytest-mock==3.15.1
factory-boy==3.3.3
faker==40.36.0
freezegun==1.5.5
