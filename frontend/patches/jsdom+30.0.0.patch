diff --git a/node_modules/jsdom/lib/jsdom/living/css/helpers/font-sizes.js b/node_modules/jsdom/lib/jsdom/living/css/helpers/font-sizes.js
index 48b90f9..d632855 100644
--- a/node_modules/jsdom/lib/jsdom/living/css/helpers/font-sizes.js
+++ b/node_modules/jsdom/lib/jsdom/living/css/helpers/font-sizes.js
@@ -113,8 +113,11 @@ function resolveLengthInPixels(elementImpl, size, dimension, isFontSize) {
         format: "computedValue"
       });
     }
-    const [, value] = FONT_SIZE_REGEXP.exec(resolvedSize);
-    return Number(value);
+    const calcMatch = FONT_SIZE_REGEXP.exec(resolvedSize);
+    if (!calcMatch) {
+      return Number.NaN;
+    }
+    return Number(calcMatch[1]);
   }
 
   const match = LENGTH_REGEXP.exec(size);
